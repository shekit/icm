class Planet {
  
  int radius;
  color c;
  float orbit_radius;
  float angle;
  boolean has_moon;
  int num_moons;
  
  
  Planet(int tempRadius, color tempC, boolean tempHasMoon) {
     radius = tempRadius;
     c = tempC;
  }
  
  
  
}
